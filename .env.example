# Optional: shows real Google Maps photos on the cards (owner photos first).
# Without it the app uses the bundled images. See README → "Real photos from
# Google Maps". Copy this file to .env.local and paste your key.
VITE_GOOGLE_MAPS_API_KEY=
